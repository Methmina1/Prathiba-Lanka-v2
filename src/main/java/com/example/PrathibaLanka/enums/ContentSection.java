package com.example.PrathibaLanka.enums;

/**
 * Editable blocks of the public site. One row per section holds the JSON the matching page renders.
 */
public enum ContentSection {
    ABOUT,
    CONTACT
}
